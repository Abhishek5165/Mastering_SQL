SELECT * FROM college.students
WHERE age>=20
--  we cannot use the where clause more than one time in the 
-- SQL query .
/* but we can use the where clause with two aur more 
two conditions */
WHERE city <> 'LUCKNOW';    -- city NOT Equal to LUCKNOW